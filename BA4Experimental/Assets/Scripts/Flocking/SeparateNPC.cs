using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SeparateNPC : MonoBehaviour
{
    /*
    //[HideInInspector] public List<GameObject> fishList = new List<GameObject>();

    void Start()
    {
        
    }

    void Update()
    {
        foreach (GameObject fishList in fishList.ToArray())
        {
            if (fishList != gameObject)
            {
                if (Input.GetKeyDown(KeyCode.K))
                {
                    Destroy(fishList);
                }
            }
        }
    }

    */
}
